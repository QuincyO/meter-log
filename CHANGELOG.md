---
title: CHANGELOG
type: index
aliases: [Changelog, Change Log]
tags: [changelog, project/meter-log]
created: 2026-07-26
---

# Changelog

What shipped, newest first — one line per day, each linking to that day's page in
[`changelog/`](changelog/). `AGENTS.md` and `ARCHITECTURE.md` describe how the app works
*now*; this records how it got that way, and the bug or the reasoning behind each change,
which a diff alone doesn't carry.

**Dates, not version numbers.** There is no release process: GitHub Pages serves the repo
root and CI deploys `Code.gs`, both from `main`, so a merge to `main` *is* the release.

**Adding an entry?** Write the day page and add its row here, in the same commit as the
change — see `AGENTS.md` §"Standing workflow rules". The automated
`Nightly sheet export — <date>` commits are the spine writing its own Sheet snapshot back to
the repo, and are not listed.

> In Obsidian: `Ctrl+O` and type a date to jump straight to a day, or open a row below.
> The vault root is the repo root, so every page here is a real note.

## 2026-08

| Day                                   | What shipped                                                       |
| ------------------------------------- | ------------------------------------------------------------------ |
| [2026-08-14](changelog/2026-08-14.md) | **Outage: Google silently revoked the script's authorization overnight** — every phone 403'd on `/exec` (Google's Drive "You need access" wall, upstream of `doGet`, so token and code were never suspects) and the 3am `exportSheetToGithub()` trigger died with it, its missing commit bracketing the outage window before any console was opened. A successful CI redeploy with the manifest's `ANYONE_ANONYMOUS` changed nothing — which itself eliminated the deployment-access hypothesis — and the discriminator was the owner opening `/exec` in a signed-in browser: a consent screen, and granting it restored service on the next request. ~15 h outage, all overnight; the phones' offline queues carried the field through (standing rule: during a spine outage, never clear site data). No email, no security event, cause of revocation unknown; `DEPLOY.md` §Troubleshooting now carries the 10-second test. **And Navigate is now the installer's switch rather than the source's** — pin-vs-address has been answered both ways in the field (address-first until the 13th, pin-first since) because both are right some days: the pin is the exact coordinate the route was built around, the address lets Maps geocode it with knowledge our lookup didn't have, and which wins varies by day and by address. **Route tuning ▸ Navigate by address instead of map pin** (`localStorage['wlNavBy']`, default `'pin'`, so an untouched phone is unchanged) now governs the worklist 🧭 and the Drive screen's Navigate together — one toggle, because it is one function and one act — with a hint under it explaining why you'd switch, since two states that look identical from the outside is a toggle nobody flips on purpose. It picks a **preference, not a rule**: each mode falls back to the other when the order hasn't got it, so Navigate never becomes a dead button. The **parked carve-out holds in both modes and is structural rather than conditional** — the `isParked` gate stays on the line *above* the branch, so a `geoFail`/`geoAmbig` order simply has no pin to prefer and neither mode can steer by the pin already known to be wrong; gate it inside one arm and "coordinates mode" quietly starts driving to known-bad pins, which is now pinned by a test asserting the gate *precedes* the branch. Device-local, saved on tap, and deliberately not routed through `save()` — whose toast promises an Upload that would not carry it. The accessor lives in `worklist-tuning.js` rather than beside `destOf` because `worklist.js` already imports that module and the reverse would be a cycle. **No `sw.js` CACHE bump**, on the 13th's reasoning. Also recorded from the verification drive: a page loaded straight at `#tuning` never *opens* the screen, so every control reads as its markup default and a stored-value check fails for reasons that have nothing to do with the code — the pre-existing *Show driving stats* toggle is the discriminator; and, noticed but deliberately not fixed, `pullVal(store.get('wlCommutePull'))` returns **0** rather than the documented 70 on a phone that has never saved the dial. **And then that toggle took the whole capture page down** — reported as *"the End of day button does not work"*, which was true and wildly understated: binding the new `#tuneNavByAddress` unguarded meant a phone holding the new `worklist-tuning.js` and the **previous** `index.html` (which stale-while-revalidate makes an ordinary state for an open or two, and a permanent one where the background re-fetch keeps failing) threw a `TypeError` inside `initWorklist()` — called from `capture.js` at **module top level**, so every handler below that line was never bound. A headless repro against the previous markup measured **16 of 20 dead**, `logStop` included, and `#refreshApp` with them, so the documented escape hatch was itself inside the blast radius. Fixed in two independent layers: the element is now bound through a null-checked local, **and** `initWorklist` is wrapped in `try`/`catch` so a broken worklist screen can never again cost the capture form and the close-out — contained, not hidden (a `console.error` with the real stack plus an amber notice naming ⟳ Force update). The rule it earns, now in `AGENTS.md`: **an element in a page's markup is a cross-file contract with the module that binds it**, so the "bump `CACHE` when you add a module or stylesheet" rule was too narrow — adding a new *element that JS binds* is the same hazard, because the service worker refreshes the two halves independently. **And the `CACHE` version is now never bumped at all**, by standing owner decision recorded in `AGENTS.md`: updates reach the field manually, when the installer presses ⟳ Force update, so a phone may sit on older bytes indefinitely and by choice — which promotes defensive binding from a one-release courtesy to a permanent requirement, since there is no atomic-shell escape from skew. Plus the meta-lesson: **don't infer the blast radius from the symptom** — forty dead buttons and one dead button look identical to a crew that only pressed one. **And the End-of-day branch finally reached `main`** — the day's tally never pasted because it had never shipped: `ecbc0ac` was written on the 13th and left on its branch, so with Pages serving `main` the phones never held a byte of it (`merge-base --is-ancestor` said no; `tallyBlock` and `#copyTally` were both absent from `main`). Second time in two days, and the warning was *inside* the unmerged commit — its own day page records the Navigate fix being "shipped once from a feature branch and therefore not shipped at all" — so the standing move is now to check `git merge-base --is-ancestor <commit> origin/main` **before reading any code** when a recent feature "isn't happening". Merged with its three other unshipped fixes (the review no longer eats typed deductions — `saveTravel` was posting the emptied set over Sheet rows; the PDF's DELAYS grid no longer prints blank when Finish outruns the prefetch; `await flush()` is a real barrier) plus 07:30/16:00 Start/End defaults. **The merge then re-introduced the morning's outage one element later**: the branch predates the fix, so `#copyTally` was bound bare above `finishDay`, `genLog`, the nav, `saveSettings` and `#refreshApp` — ~13 handlers that would have died on a phone holding the new `capture.js` and yesterday's markup, reported as *"the Finish day button does not work"*. Caught before push, guarded, and pinned by a test that now forbids **either** recent id from being bound bare above `#refreshApp`, since a handler that can unbind ⟳ Force update takes the only route code has to these phones. The shape worth keeping: **a merge is a deploy, and a branch written before a rule does not know about it**. **Then the first real paste came out as `Dispatched:%0AInstalled%3A%2017%0AUTI%3A%204…` in Messages** — and nothing in this repo encodes anything: `copyDayTally` hands the raw string to `writeText` and decoding the paste returns the tally byte-identical. The tell was the asymmetry — `Dispatched:` kept its literal colon while every *later* colon became `%3A`, which is a URI being serialized, first token as scheme. It is one: `new URL("Dispatched:\nInstalled: 17…")` parses with protocol `dispatched:`, because a bare `Word:` at position 0 is a **valid URI scheme**. iOS's pasteboard sniffs that, publishes a `public.url` flavour beside the plain text, and Messages pastes the percent-encoded `absoluteString` while an app taking the plain-text flavour shows it perfectly — the installer's own *"it pastes fine into the other app"* was the proof there were two flavours before any source was read. **Every obvious fix fails and the tests now say so**: reordering doesn't help (`Installed:` is equally a scheme — the whole `Word: value` format is scheme-shaped), filling the value in doesn't (what follows the colon is irrelevant), and a leading space or newline is trimmed before parsing. What works is a first line that cannot *begin* a scheme, so the block is **dated** now — `YYYY-MM-DD` starts with a digit, which RFC 3986 forbids there, defeating the sniffing structurally rather than by a trick and answering which day the numbers are for. The date is captured beside the stops it describes (a phone left running past midnight must not date yesterday's counts as today) and falls back to `localDate()` rather than emitting a blank line, since a blank first line would be trimmed and hand the bug back. General rule for anything this app copies: **text beginning `Word:` is a URL as far as iOS is concerned** |
| [2026-08-13](changelog/2026-08-13.md) | **Hold ＋ Add order to paste a whole list of work order numbers** — the worklist is built numbers-first and addresses-after, but only the address half of that workflow existed; a two-second hold on ＋ Add order (the Optimize button's own tap-vs-hold binder, bound a second time) now opens a paste sheet taking one WO# per line, with parse + dedupe as a pure `bulkAddPlan` in `worklist-dedup.js` — CRLF-safe, blank lines ignored without inflating the count, duplicates of non-done orders skipped and reported in the toast, a done order's number allowed back for the revisit. Bulk-added ids ride `addToQueue` → `offerAddTo` like any single add, so a locked day raises one "N orders added" chooser instead of being bypassed, and the addressless orders sink to "Needs address" where 📝 picks them up unchanged; no spine change. Two traps recorded: press-hold's eat-one-click flag assumes the browser's synthetic click, so a headless drive dispatching untrusted pointerups must also dispatch the click or the *next* programmatic tap is silently swallowed (reads as "the tap path is broken"); and the SW CACHE version is pinned in **three** test files (`drag-autoscroll` too), not the two you remember. **And 🧭 Directions / Navigate now drive to the order's pin instead of its address** — handing the maps app an address string is a request to geocode it a second time with its own guess (wrong side of a lake, one of three townships sharing a road name), while the pin is the exact coordinate the route was built around and the one the crew sees on the route map, so `destOf` returns `lat,lng` first and falls back to the address text only when there is no trusted pin. The old precedence was a documented decision, not an oversight — *"a mis-geocoded pin must not steer the truck"* — and what survives of it is the carve-out: a **parked** order (`geoFail` 📍 / `geoAmbig` ⚠) keeps a last-known pin that `route.js` never blanks and that is exactly the pin known to be wrong, so the gate is `isParked(item)` rather than `coordsOf(item)` alone, reusing the repo's single "may the truck route on this pin" answer instead of writing a second one. The clipboard half is untouched — the card's unit + address line, still copied synchronously before the iOS scheme hand-off, and the destination string never reaches it. Shipped once from a feature branch and therefore not shipped at all — Pages serves `main`, so ⟳ Force update faithfully re-downloaded the old bytes and the crew still drove 35 km south off the address string while a correct pin sat on the route map; `AGENTS.md` now says a feature-branch commit has not shipped either. **No `sw.js` CACHE bump** — briefly bumped v42 → v43 and put back on the installer's call: a bump makes every phone re-cache the whole shell at once on its next open, so a bad change would be bad on all of them simultaneously, on boat signal, unasked; the rollout stays per-phone via ⟳ Force update. Two things recorded with it — changing the constant in *either* direction is a new worker and one full `addAll(SHELL)`, so a revert costs the same wholesale re-cache it reverts, and *not* bumping is not a gate either, since stale-while-revalidate hands a phone the new `worklist.js` within an open or two regardless. **And End of day stops throwing the review away, with the PDF finally printing what the phone is holding** — a field report in three parts, two of them the same slow afternoon: `loadDay('eod')` paints the review twice (cache, then `day`+`idle` on a timeout-less `apiGet`), and the second paint replaced `eodGaps` wholesale and rebuilt every card collapsed, so a response landing ten seconds in ate the minutes just typed — and because a `.sheet` is a bottom-anchored backdrop, the moving card re-aimed the tap in flight at the dim area, which `closeSheets()` read as a dismiss (*"it kicks me out"*). `eodTouched` now freezes the review the moment anyone works in it and the backdrop dismiss requires its own pointerdown; the rebuild was **data loss**, not chrome, since `saveTravel` replaces the day's gap-tagged rows and an emptied `eodGaps` posted over deductions already on the Sheet. The blank delays were one line of omission: `buildLocalSummary` fed `pendingTravel` to the gap math but returned the untouched `downtime` array, and every delay figure on both templates reads that array — so a Finish that outran the 2s-debounced prefetch drew a blank DELAYS grid while the same minutes reached the Sheet a second later (*"wait like 30 seconds before I'm safe to submit"*). It synthesizes the rows `saveTravel` would have written, dropping the existing gap-tagged ones first (whole-day replace ⇒ never double-count) and gated on a non-empty pending list so the printed rows and the netting always describe one set. Underneath both: **`await flush()` was never a barrier** — `if(flushing) return;` resolves instantly and `enqueue` fires an un-awaited flush, so every drain-then-read-the-Sheet call site was reading a Sheet missing rows the phone had just queued; it hands back the in-flight promise now, must never reject, and End of day opens before draining. Plus two asks: **Start/End default to 07:30/16:00** (own constants — never `ROUTE_DEPART_TIME`/`ROUTE_DAY_END`, whatever the 16:00 coincidence; a saved time still wins and `edit.html` stays blank), and **every PDF copies the day's tally** — `Dispatched:`/`Installed`/`UTI`/`TR`/`EER`, Dispatched empty on purpose, EER the `Electrical Repair` UTIs counted on *both* lines, TR the worklist's timed appointments (the only place an appointment exists), fired synchronously at the top of each handler off a pre-read cache because a `writeText` after an await is refused on iOS, with a 📋 Copy tally button as the guaranteed path |
| [2026-08-11](changelog/2026-08-11.md) | **The SSD bundle's restore script comes home, and it no longer assumes CH** — the planner came up with both readiness badges offline and it looked like its files had been deleted; they hadn't, Docker had been wiped (`Images=0, Containers=0`), and since the planner is a thin client over local OSRM and Nominatim, losing the two services looks identical to losing the page. Recovery should have been one command off `F:\geo-bundle`, but the repo's `scripts/setup-geo-on-new-pc.ps1` had been committed once in `7ef68a8` and never updated while the copy on the SSD was quietly fixed during an ARM64 setup — the same drift `CLAUDE.md` exists to prevent, one directory over. The repo copy is now the SSD copy: it picks `ch` or `mld` by testing for `<base>.osrm.hsgr` instead of hardcoding `--algorithm ch` (`D:\osrm` holds the MLD set and no `.hsgr`, so the old line would have started a container that fails to load the graph and exits), names the containers so a re-run can replace them, builds the emulation-safe Nominatim frontend on ARM64 hosts, checks `$LASTEXITCODE` rather than a `try/catch` that never fires on a native exit code, and polls both endpoints before claiming success. **That last one produced a false negative on this very restore** — both waits timed out while both containers were healthy, because the 5.3 GB image load had saturated the disk and the MLD graph was still loading, so `DEPLOY.md` now says that "setup finished, but not everything came up" is not proof of failure and `docker ps` + OSRM's `running and waiting for requests` are the real signal. `DEPLOY.md` also gains the preprocessing/run-flag pairing rule next to the `osrm-routed` command (the from-scratch steps were never wrong, they just never mentioned MLD) and a recovery section for a Docker wipe on the PC that already worked, noting that Nominatim's imported DB lives *inside* its image and so must be reloaded rather than restarted. One more trap recorded: an unrelated app (`Salad.Bowl.Service`) was holding port 5000 and answering the probe with a bare 404, which reads as "offline" on the badge and blocks the container from binding — worth one `netstat -ano \| findstr :5000` before anything else |
| [2026-08-10](changelog/2026-08-10.md) | J# capture fields self-normalize to `J` + 7 digits on every scan: a wrong barcode wipes the field so you can scan again, and a re-scan replaces the old value instead of appending to it — no button, no wrong-format popup. **And the daily-log PDF's blank bottom now prints the day's notes** — a **Notes** block fills the white space under the totals on both templates with the end-of-day, downtime and per-stop remarks, skipping the `gap HH:MM–HH:MM` machine tags the travel review parks in the downtime note column; the typed end-of-day note is overlaid on the client before render (the online preview/prefetch paths never carried it, the same bookend trap), and an `edit.html` reprint falls back to the saved `Tracker` note. **And the analytics page finally shows the `InstallerMetrics` tab** — a new **Installer lifetime metrics** section on `map.html`'s Analytics view: per-installer all-time table (days, hours, installs, UTI %, downtime/day, pace) with a recent-30-workday trend arrow, a land-vs-boat avg-minutes-per-log chart (land runs ~2–3× faster), and the dwell model behind the route planner's ETAs with its gps/fit/pace source badge; honestly labelled lifetime since the read has no date window — the page's date range doesn't apply, the installer chips do; each card carries a tap-to-open ⓘ explainer that decodes the jargon, including how the three dwell sources are measured. **And a GPS dwell sample now requires a log inside the parked hole** — any 2–120-min hole between recorded driving legs used to count as time installing, which is also what lunch looks like, and at the 15-sample bar a few of those move the median that outranks the fit; production data proved the point: 5 of the 6 existing qualifying holes contained no log at all (a pre-first-log morning stretch), and a 2-min pull-away grace keeps the drive-off-then-tap sample alive. **And the recorder now splits a leg at every real stop** — a recorded day used to be deliberately one continuous leg (even an OS kill rejoined it), so the between-leg parked holes the dwell model measures never existed in normal use; `stillCheck` ends the leg at the arrival point once the truck has sat ≥2 min within 40 m and then moves off, making legs park-to-park and a normally recorded day worth ~one sample per stop. **And the validating log must also be near the parked truck** — within 300 m of the previous leg's last polyline point, decoded spine-side by a hand port of `decodeTrack` — with the check skipping itself when a leg has no polyline or a stop no pin, so it tightens without killing legacy samples |
| [2026-08-04](changelog/2026-08-04.md) | **Boat work is back, and so is the Boat/Land switch — in Settings, on a new key, defaulting to land** — the [July 31st](changelog/2026-07-31.md) removal argued that a mode a crew sets a few times a year does not belong next to the sync pill where one accidental tap retags a whole day's writes, and that half of it still holds, so the switch returns as a two-up pill in **Settings ▸ Work mode** rather than to the top bar: saved on tap like the other device-local preferences, sticky until changed, `LOCKED_MODE` deleted outright rather than set to `null` because a dead constant naming the lock is a re-lock waiting to happen. **The key had to be renamed or "default to land" would have done nothing** — boat was the *original* default under `localStorage['workMode']`, so every phone in the field carries a literal `workMode='boat'` rather than an absent key, and a default never fires on a key that is already set; re-reading it with a land fallback would have brought most of the fleet up in **boat** on the first load, invisibly, which is the same trap July described from the other side. The mode moves to a fresh `captureWorkMode` (the legacy key swept once on load and never read again), the polarity flips so blank/absent/mangled all resolve to land, and the boot path is `applyModeUI(); paintModeSeg();` and deliberately not the pre-lock `setMode(workMode())` — that line wrote the key back on every load, which is exactly how a fallback ended up baked into every phone as a literal value. **And it exposed a bug the lock had been hiding**: `eodStateKey()` fingerprints the gap allocations, both bookends, the notes and the include-delays checkbox but *not* the mode, which is what picks the PDF template — so opening End of day, flipping the mode in Settings and tapping Finish reused the cached summary and handed the crew a land PDF for a boat day with no error anywhere; guarded twice, since it fails silently. Three other mid-flip behaviours are deliberately left alone and documented where they live — a gap list already on screen isn't rebuilt (land gets a zero-length lead gap, but re-running `loadDay` would discard deductions already typed), a drive leg in flight keeps the mode it started in, and queued rows keep the mode they were logged under. No data-model change: `workType` never stopped riding every payload, `normWorkType` never stopped reading blank as boat, and `dailylog.js` never lost the boat template. `teams.html` keeps its own `teamsViewMode` — now that *both* pages have a switch that separation matters more, not less, being the only thing stopping an admin flipping the office view from repainting a crew's phone — and the three `installerMetrics` reads pinned to `workType:'land'` stay pinned, now with a test, because routing is a land workflow whatever the capture screen says |

## 2026-07

| Day                                   | What shipped                                                       |
| ------------------------------------- | ------------------------------------------------------------------ |
| [2026-07-31](changelog/2026-07-31.md) | **A 🔓/🔒 toggle: the day is settled when the installer says it is** — this was the fourth change in a week to "which orders are on today", so the mechanism that kept needing fixing was the *inferring*, not any of the inferences; unlocked, the meters/day target and the plan date are live and 🧭 Optimize re-plans every day at whatever number is in the box, and locked, day 1 is frozen exactly as it stands and nothing moves work in or out of it — not a logged stop, not a Download, not another Optimize — until it's unlocked by hand, with an added order raising the same *Add to today / Leave for later* question and slotting in without pushing anything off the end. `needsCommit`, `dayCapacity`, `opts.max`/`opts.fromTags`/`opts.replan`/`opts.resize` and the whole `freeReplan`/`midReplan`/`exhausted` triangle are deleted; the press is the only commit there is. The lock is **one date** (locked iff it equals the plan day), so a new day releases it with nothing to expire. **Unlocked is deliberately not "no `day1Count`"** — `applyTodayAnchor` also runs after every logged stop, on the Drive screen's automatic refresh, on Download and at boot, and re-chunking there would refill day 1 from the front of pending for anyone who simply hadn't pressed 🔒, which is yesterday's bug back on a timer; there are three answers, and only the meters/day box asks for the re-chunk. The variant switch and the drag write-back carry the count too, or the lock leaked through both. The office gets the same toggle on the planner, synced by one appended `WorklistPlans.dayLockDate` column whose blank is a **real value** (so the spine guards on key presence, not truthiness, or unlocking from the phone would never reach the sheet). **And the road-distance button stops answering with the whole week** — two orders 2 km apart with the tile reading 241 km, which was correct arithmetic over a four-day plan with one far-off order at the bottom of the list, summed into the figure a crew reads as today's driving; nothing was double-counting a commute (a day's first stop has always been charged 0 and the drive out lives in `homeLegMeters*`, which no total reads), the headline was simply unscoped. The phone's tiles and counts line price **day 1** now, and the counts line takes its metres from `liveDayMeters` — the same live `day` field the day dividers bucket on — so the headline and Day 1's divider are the same number *by construction* rather than two sums that happen to agree; the tiles bucket on each variant's own saved day, so road-vs-straight stays a like-for-like choice. The office keeps the whole plan and now says so (`241 km (road) · 4 days`), and the phone gains a caption carrying what its tiles leave out, because there is nothing to hover on a phone. Two comments that described the opposite of their code are fixed with it — including the day divider's own tooltip, which told the installer the drive out was in the number beside it. **And the Drive screen's automatic refresh moves 5 → 3 minutes and starts timing your arrival from where the truck actually is** — every remaining ETA hangs off "where does the next drive start?", and the answer had been the pin of the last order *completed*, which is right in a driveway and wrong mid-drive or after any walk-up that never matched a worklist order; the GPS recorder had been watching all along and simply never let the coordinates out of the module, so the origin is a ladder now (live fix → last completed pin → muster point) and a live fix moved a seeded first-stop ETA from 08:59 to 11:08 on position alone. The 2-minute freshness bound is the feature rather than a knob: a web app gets no GPS in the background, so a Maps hand-off freezes the newest fix at the driveway the crew pulled out of and it stays there looking perfectly good for the whole leg — past the window it decays back to the old origin. `routeTravel` re-prices the **first** leg from the same fix so the arrival time and the *Route done ~* clock aren't two models of one day, and the Drive card finally **shows** the number — `Arrive ~11:08 · in 129 min`, off the persisted `scheduledEta` rather than a second estimate, greyed and labelled `(stale)` when the last solve failed. A phone that never armed tracking reaches none of it and behaves exactly as before: no prompt, no GPS wakeup, nothing on screen. **And the Boat/Land switch comes off the capture page** — boat work has wound down, so a control that can only be set wrong is worse than no control; `js/work-mode.js` **forces** the value rather than defaulting to it, which is the whole change, since boat *was* the default and every phone in the field therefore has a literal `workMode='boat'` a new default would never consult. The drive recorder was the second reader of that key and cannot import `capture.js` (which imports *it*), so the lock lives in a module of its own — left alone it would have kept tagging every drive leg blank, which the spine reads as boat. The teams admin page keeps its switch on its own `teamsViewMode` key, because the office still has to reach historical boat teams and flipping the office view should never repaint a crew's phone. **And the Drive page's arrival times stop being measured in straight lines** — the phone already routes on a downloaded district, so 🧭 Optimize produced ETAs measured on real roads and then `applyTodayAnchor` (after *every* logged stop, on the 3-minute refresh, on Download, at boot) re-timed the whole day with haversine × 1.3 ÷ 50 km/h, which meant every re-anchor was a downgrade and the good numbers lasted until the first meter was logged; `estimateTravel` is a ladder now — the district's road graph, else the road metres the last solve saved, else crow-flies — and on eight fully-routable stops the day's last arrival moved **12:44 → 13:41**, +1 min at the first stop and +57 by the eighth, which is the shape of "fine in the morning, nonsense after lunch" and the optimistic direction that fails late rather than early. Coverage is judged on the **stops**, never on the truck, so a fix at the edge of the district can't veto a day the pack covers; `loadGraph` takes `{switchActive:false}` because it is not a pure read and a background re-anchor must not silently re-point the crew's map; and `wlTimesEstimated` now describes the **model** (`travel.measured`) rather than the caller, which retires two Download-time writes that were cleared on the line before an `applyTodayAnchor` that immediately re-derived them. `routeTravel` re-prices its first leg through the same graph, so the arrival time and the *Route done ~* clock are one model rather than two that agree on a good day. Between two Optimizes the pending set only ever shrinks, so the stop×stop matrix is cached and served by index remapping and only the truck's row is re-measured — 266 ms vs 12 ms on the committed pack, with `matrixRow` sharing `fillRow` with `matrix` so a warm cache can never change an ETA. **And the pace card stops telling a crew who are on track for twenty that they will finish thirteen** — at 12:31 with 13 done it projected *zero* of the eight stops still ahead over the next three and a quarter hours, directly above a card reading "Arrive 12:36 · in 5 min", while the route's own ETA chain landed the last of them at 14:45. Both of its inputs were wrong and in opposite directions, which is why it read as merely gloomy: remaining travel was 110.8 km ÷ the HUD's "average moving speed", which counts the crew standing at a property as driving and had sagged to 28 km/h, and on-site was the 16-minute median gap less a flat 10-minute drive over legs really driven in two, floored back up to 8. The 13th stop was an off-plan walk-up, which made it a controlled experiment: `done` rose while the route ahead did not change by a metre and the remaining drive still went 196 → 241 minutes, so the longer the crew spent installing the fewer installs they were promised. The 25 km/h floor added the day before did not fire because the number that beat it was 28, and raising it only moves the number that beats it — effective speed climbs with leg length, and this route ran 2.2 km a leg around Kearney in the morning and 13.9 with a 58.7 km haul in the afternoon. Both halves are priced by one `estimateTravel` lookup now, the same ladder the arrival badges use, over a frame carrying the done orders as well as the pending ones so the legs already behind the crew can be priced at all; each gap's own measured drive is netted out instead of a constant, so on-site falls out of the arithmetic rather than being guessed and then floored. `avgMovingSpeed` is left alone — the map viewer and `installerOnSiteFromTracks` read it — it just no longer prices this card. On the real day: `~13 · Route done 5:36 pm` → `~19 · 3:59 pm`, and the finish clock now lands on the last stop's own ETA instead of 74 minutes past it. **And then, with both inputs trustworthy, the card still said nineteen on a day worth twenty-one** — it deducted the *whole* remaining route's driving before asking how many stops fit, so the 23-minute run out to the last stop, which the same calculation had just decided was unreachable, was subtracted from the time available for the three that weren't. That division is exact only when the crew drives the entire route, which is the one case where the answer is already capped and the arithmetic doesn't matter; everywhere else it charges the stops you can reach for the drive out to the ones you can't, and it fails worst on the ordinary shape of a rural day — a tight cluster near town and one long run home. The count is a walk of the chain now, leg and stop at a time until the clock passes the horizon. The finish clock keeps its closed form on purpose: reaching the last stop means traversing every leg, so the sum and the walk agree there — which is why *Route done ~4:14 pm* was already right on the very card whose count was wrong by two |
| [2026-07-30](changelog/2026-07-30.md) | **The work list you download at the start of the day stays the day you committed to** — the meters/day target sized Day 1 live against the meters actually installed, and since the target counts meters while the list counts orders, a two-meter order or a walk-up pushed the tail of a committed day out to tomorrow by mid-morning (and a *met* target pushed the whole remainder out, taking the pace card off the screen with it), while a finished day rolled tomorrow's chunk up into today by itself, all of it re-run every 5 minutes by the new auto-refresh; Day 1 is the committed set entire now, the target sizes the day when the day is *planned* and then lets go, and work joins a day already under way only by hand — 🧭 Optimize, the Add-to-today sheet (which loses its tail-rolling middle option and now works on a cleared day), or the office's own Day 1 on a Download; installing extra reads as "· 4 over your 24" with a later *Route done ~* instead of as a shorter list. **And** the Drive screen's "Route done ~" clock stops reading as a time that has already passed — it was a bare 12-hour label on a number with no ceiling, so a real 11:29 **at night** printed as `11:29` on a card being read at 11:36 in the morning, and it carries am/pm now (plus a day marker past midnight); the day's on-site pace is the **median** of its gaps instead of the mean, so one long hold-up costs a single gap rather than wrecking every projection until midnight, and any delay the crew did log is netted out instead of being counted as time spent installing; and a day-average "moving speed" dragged under 25 km/h by on-foot minutes at the meter no longer prices the remaining route, which had been charging the afternoon 2¼ hours of driving for 45 minutes of road |
| [2026-07-29](changelog/2026-07-29.md) | The phone's 🧭 Optimize tap now requires the district to be downloaded — no more silent straight-line routes that look solved and ignore every river and dead end — with the button greyed and the reason printed before anyone presses it; the two-second network hold stays exempt as the only second opinion an on-device route has, and the desktop planner is untouched. **And a duplicate J number now warns instead of discarding the stop** — logging a New or Old J# already on one of your own stops from the past week blocks the tap with a card naming the order it's on, offering *Go back and fix it* or *Log it anyway*, checked on-device (so it works offline) and again on the spine; the old path returned before `appendRow` and told the installer "Entry discarded", which destroyed real field work. **And the Drive screen now refreshes itself from the sheet every 5 minutes** — gated on drive tracking being armed, the *Show driving stats* opt-in, and being on that screen — pulling the list and today's stops and then re-anchoring, which is the part that actually re-times the route; a timer never toasts, never confirms, never touches the capture form, and never resurrects an order already logged on this phone |
| [2026-07-28](changelog/2026-07-28.md) | The drive-mode pace is measured against the meters/day target instead of a day that was already shrunk to fit, so it can report being behind again; a phone the crew only drives by finds out what the logging phone has done; and a route re-optimized at noon departs on the clock the crew is actually on, with the remaining ETAs re-timed from the last completed stop after every meter; the Drive screen drops the near-duplicate Target gauge and its one remaining card now says what time the route itself is done; and the gauge goes back to measuring the route rather than the target — with the arithmetic showing why the morning's reasoning for the swap was wrong, and the day-2 chunk it was quietly pacing once the target was met |
| [2026-07-27](changelog/2026-07-27.md) | Route map keeps its road lines all day; the Optimize press picks the matrix ladder; a raised meters/day target can unfreeze Day 1; on-site time is measured per stop instead of guessed; the phone can plan a route for a day that isn't today, and a stale lock stops taking the whole route down with it; Finish By is scrapped so meters/day alone sizes a day; the Optimize sheet stops claiming straight-line over a road-routed run; a typed meters/day survives without blurring the box, and changing it re-splits the days on the spot instead of waiting for an Optimize; the day divider's length is read back from the route's own ETAs instead of a historical cadence plus an hour; the measured on-site block stops being stored as a 1900 date, which is why no phone had ever used it; an appointment is placed against the day the route will really drive instead of a placeholder one, arriving early and waiting always beats arriving late, and an ETA left over from a rejected route now says so |
| [2026-07-26](changelog/2026-07-26.md) | Offline road maps, offline geocoding, districts drawn on the planner; graphify wiring; district builds fixed, with a progress bar; districts you can remove, grow and hold several of; turn-by-turn off the offline pack |
| [2026-07-25](changelog/2026-07-25.md) | Walk-up meters count toward the day; start-location popup          |
| [2026-07-24](changelog/2026-07-24.md) | Route tuning dials, real ETAs, the drive HUD, four field bugs      |
| [2026-07-23](changelog/2026-07-23.md) | Drive mode, two route variants, one instruction set for all agents |
| [2026-07-22](changelog/2026-07-22.md) | Optimize turned on for everyone; per-installer metrics             |
| [2026-07-21](changelog/2026-07-21.md) | Google Routes + ORS road matrices; the desktop planner             |
| [2026-07-20](changelog/2026-07-20.md) | Route optimization arrives; worklist sync; a long bug-fixing day   |
| [2026-07-19](changelog/2026-07-19.md) | Sub-foremen and a per-sub reports page                             |
| [2026-07-18](changelog/2026-07-18.md) | Land-work mode; removing a stop became archive-before-delete       |
| 2026-07-04 → 07-17                    | *Quiet fortnight — nightly Sheet export only*                      |
| [2026-07-03](changelog/2026-07-03.md) | A performance pass for scale: caching, locks, date windows         |
| 2026-06-30 → 07-02                    | *Nightly Sheet export only*                                        |

## 2026-06

| Day | What shipped |
|---|---|
| [2026-06-29](changelog/2026-06-29.md) | Accuracy-driven GPS; instant end-of-day PDF |
| [2026-06-28](changelog/2026-06-28.md) | Timestamps stop lying; the PDF moves to the phone |
| [2026-06-27](changelog/2026-06-27.md) | Dispatch dedup; a steadier GPS fix |
| [2026-06-26](changelog/2026-06-26.md) | The app is modularized into shared ES modules |
| [2026-06-25](changelog/2026-06-25.md) | Storage-first: a stop survives before it's sent |
| [2026-06-24](changelog/2026-06-24.md) | Offline cache, and the first worklist |
| [2026-06-23](changelog/2026-06-23.md) | Travel time becomes per-installer |
| [2026-06-22](changelog/2026-06-22.md) | Log types, downtime, and the back-office editor |
| [2026-06-21](changelog/2026-06-21.md) | Teams take shape; CI starts deploying the spine |
| [2026-06-20](changelog/2026-06-20.md) | The spine, the map, the service worker |
| [2026-06-19](changelog/2026-06-19.md) | Shaping the capture form |
| [2026-06-18](changelog/2026-06-18.md) | Initial commit |
