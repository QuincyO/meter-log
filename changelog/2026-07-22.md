---
title: 2026-07-22
type: changelog
tags: [changelog, project/meter-log, area/routing]
created: 2026-07-22
---

# 2026-07-22 — Optimize goes live; metrics drive the day split

## Per-installer metrics + a target-driven multi-day route split

`7ab61a5`, `6f5eb91`

Metrics are kept per installer and split into boat / land / combined columns, so a crew's
real pace sizes their days rather than a fleet-wide average.

## Worklist Optimize turned on for everyone

`efe0df0`, `ee1f0aa`, `8d8126f`

Straight-line distances for the ordinary run — free, no matrix call — with the road-matrix
run kept behind a gesture so it can't be triggered by accident. First a 5-tap secret, then a
two-second hold, then a CSS fix to stop the hold selecting the button's label. (That guard
grew a `touchstart` `preventDefault()` which caused the scroll bug fixed on
[2026-07-26](2026-07-26.md).)

## Mobile worklist route editor

`bc5814e` — reorder and fix a route from the truck, not just from the office.

## Timed appointments and locked route slots

`ced5710` — an order that must happen at a set time stops being shuffled by the solver.

## Optimized routes can start from your current location

`ecdb095` — the answer to "I hit my target and want to keep going, but the next day's first
stop is across the map".

## Planner provider health provenance and status UI

`7190574`, `a24f541`, `3e88e9f` — which matrix source actually answered, surfaced rather than
guessed at.

---

[← all days](../CHANGELOG.md)
