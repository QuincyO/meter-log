---
title: 2026-07-28
type: changelog
tags: [changelog, project/meter-log, area/frontend, area/routing]
created: 2026-07-28
---

# 2026-07-28 — The pace gauge can say "behind" again, a second phone finds out what the first one logged, and a route re-planned at noon stops departing at 8:15

One field report, two devices: the crew logs meters on a work phone and drives by a
second one. *"When I download the work list and get the orders that I've done, the pace
just sets the target to be the remaining metres — it always says I'm on pace. And when I
re-optimize it always begins from 8:15 and never takes into account what time it is now."*
Three separate defects behind it, all of them shipped here.

## "On pace" was a question that answered itself

`js/compute/estimate.js`, `js/drive.js`, `js/worklist.js`, `js/worklist-tuning.js`,
`tests/estimate.test.mjs`, `tests/pace-live.test.mjs`, `ARCHITECTURE.md`, `AGENTS.md`,
`USER-GUIDE.md`

`paceFor` decided the gauge's verdict with `onPace: (pendingCount - willDo) <= 0` —
*"will I finish the stops still on today's route?"* — and `js/drive.js` captioned it
`${done} of ${done + pendingCount} stops`.

**But today's route is Day 1, and Day 1 shrinks by exactly what has been done.**
`day1Count(anchor, day1Ids, dayCapacity(target, installedToday))` sizes it at
`target − installed`, so every completed meter took one stop off the numerator *and* one
off the denominator. The bar refilled itself no matter how the day was going, and
"behind" was not a state the gauge could reach. The installer's phrasing — "it just sets
the target to be the remaining metres" — was a precise description of the arithmetic.

The meters/day target is the only denominator that does not move underneath the answer,
so that is what `onPace` means now: `targetShort === 0`. The route shortfall survives as
`routeShort`, printed as the caption's second half ("· 2 stops won't fit"), because it is
the one thing the target cannot say — you can be on pace for 24 and still not reach every
house on the list. The bar clamps at 100%, since a day that beats its target (walk-ups,
or just a good one) is on pace and not an overflow.

**Trap worth remembering:** the meters/day number is returned **once, at the top level**
of `projectDayReal`. `paces.target` is already the finish-by *horizon*, so echoing it onto
each pace would have produced `paces.target.target` — two unrelated meanings of the word
one dot apart. `js/drive.js` reads `est.target`; the two only meet inside `fillPaceRow`,
where there is a comment saying so.

## The driving phone believed nothing had been installed all day

`js/worklist.js`, `tests/pace-live.test.mjs`, `ARCHITECTURE.md`, `AGENTS.md`,
`USER-GUIDE.md`

The other half, and the reason the report said the caption read **"0 of N"**. Everything
downstream of "how much has been done today" — `projectDayReal`'s `done`, `tallyOn`'s
`installed`, and therefore `dayCapacity` — reads `dayCache[`name|today`]`. That cache is
only ever filled by `cacheRecentDays`, which ran at page load and on reconnect.

**A phone the crew only drives by never logs anything, so nothing on it ever invalidates
that cache.** Open since morning, it saw zero installs until it was reloaded: `done = 0`
made the gauge structurally incapable of reporting a shortfall, and `installedToday = 0`
handed Day 1 a full fresh target. `drive.js` repainting the pace every four seconds just
re-read the same stale copy faster.

`wlDownload` now pulls today down (`cacheRecentDays(1)`, a single-date `range` call)
**before** `applyTodayAnchor` — a refresh after it would size the day off the stale copy
and defeat the point. `drivePace` refreshes on its own **3-minute** throttle, stamped
before the await so a slow call cannot stack up behind itself.

**The landmine:** the cache key is `${cfg().name}|${date}`. If the two phones' Settings ▸
name differ by so much as a space, none of this fires and the symptom is identical.
Check that first. It is now in the user guide as well, since it is the crew's to get right.

## A route re-planned at noon still departed at 8:15

`js/route-constraints.js`, `js/worklist.js`, `tests/route-constraints.test.mjs`,
`tests/route-depart.test.mjs`, `ARCHITECTURE.md`, `AGENTS.md`

`scheduleRouteConstraints` read `opts.firstStopTime` **once** and handed the same value to
`simulateDay` and `placeAppointments` for **every** day of the route. `ROUTE_DEPART_TIME`
is where the day starts — it is not where every re-solve of that day starts. Press
Optimize at 11:40 and every ETA on the route was a morning ETA, and the appointment
search was picking slots against a day that had already half happened.

`opts.day1FirstStopTime` overrides day 0 alone (`firstMinFor(day)`); days 2+ keep the
constant, and omitting it is byte-identical to the old behaviour — there is a test that
asserts exactly that, because it is the regression that would matter.

**The rule is about the anchor, not the hour**, and that came straight from the
installer: the muster point is a *morning* anchor, so an Optimize staged there keeps 08:15
however late it is pressed — that press means "show me the day as planned from the depot".
"Start from here" is a run happening *now*, and so is every rolling re-schedule. The
"Start from here" gate had already answered **where** a mid-day re-optimize begins;
nothing had ever answered **when**.

Two guard rails carried over from the finish-by clock that was scrapped on 2026-07-27:
the override reaches **ETAs only** — `counts[]`/`capFor`/`dayCapacity` are untouched, or
that dial is back under a new name — and it only ever moves the clock *forward*, so a run
before 08:15 keeps the constant rather than promising a 06:40 start nobody is driving.
`wlDayEta` takes the same clock for day 1, or the divider announces ~8h over a route the
badges beneath it plainly show as four.

## The ETAs now move as the day is worked

`js/worklist.js`, `tests/pace-live.test.mjs`, `ARCHITECTURE.md`, `AGENTS.md`

Also asked for: *"as the day progresses, every time I complete an order the ETA should get
updated for the next remaining metres in the route."* `applyTodayAnchor` already ran after
every logged stop, but two things silently stopped it landing.

**The write guard was blind to a changed time.** It skipped any order whose `order`, `day`
and `scheduledDate` were unchanged — safe only while the day's clock was a constant. With
a clock that moves, a stop can sit at the same position on the same date and still be due
at a different time, and the guard threw away exactly that write. It compares
`scheduledEta` now.

**And the drive-out was measured from the depot all day.** `estimateTravel` anchored on
the cached crew start, so after finishing stop 5 the ETA for stop 6 was priced as a drive
from the muster point. It anchors on `lastDonePin` — the pin of the most recently
completed order, by `updatedAt` — falling back to the crew start. Deliberately not a GPS
fix: this is free, needs no permission, and works with the radio off. Gated on
`planDay() === localDate()`, because an evening plan for tomorrow starts at the depot like
any morning does.

## Verification

`node --test "tests/*.test.mjs"` — 474/474. New coverage: the day-1 clock moving Day 1
while days 2+ hold at 08:15, an omitted override reproducing the old schedule exactly, a
late start that reports lateness instead of resizing the day, the target-vs-route
denominator with both shortfalls, a day that beats its target, and the wiring assertions
for the cache refresh ordering and the estimate's origin.

**Not done here:** the headless-browser pass in `VERIFY.md` §3. This session's sandbox
refused to launch a local server or Edge, so the DOM-level behaviour of the pace card is
covered by source assertions rather than a screenshot. Worth a real two-phone run in the
field — check the Settings ▸ name match first.

## One pace card on the Drive screen, and it says what time the route is done

`index.html`, `js/drive.js`, `js/compute/estimate.js`, `css/drive.css`,
`tests/estimate.test.mjs`, `tests/pace-live.test.mjs`, `ARCHITECTURE.md`, `AGENTS.md`,
`USER-GUIDE.md`

Straight from the installer: *"there's two cards that show pace, one for 3:45 and the
other for target — let's remove the target one. And on the new pace card, can we have an
estimated time to completion for the route: what time, based on our pace, we're gonna
finish."*

The Target card was right to feel redundant. Its horizon is `finishByMin`, and the phone
has passed the **constant** `ROUTE_DAY_END` (16:00) there ever since the per-installer
finish-by dial was scrapped on 2026-07-27 — fifteen minutes from the working-hours card's
3:45. Two gauges, two bars, two counts, fifteen minutes apart. `#dpTarget` is gone from
`index.html` and `paintPace` paints one row.

**Removed from the Drive screen only.** `projectDayReal` still returns `paces.target`,
because the plan-mode banner (`renderPlanEstimate`) and the `#tuning` what-if both read
it; there is a test asserting `js/worklist-tuning.js` still does, so a later tidy-up
cannot quietly take the model's other callers with it.

The finish clock is new but the arithmetic is not. `paceFor` already solves
`installTimeLeft = horizon − now − remainingTravelMin` for how many stops fit; read the
same three terms **forward** and you get when the last one is done:
`now + remainingTravelMin + pendingCount × onsitePerStop`. That is
`projectDayReal`'s new top-level `routeFinishMin` / `routeFinishLabel`, painted under the
caption as *"Route done ~4:20"*.

**The trap this design avoids:** sourcing that clock separately. Every term is already in
`paceContext` — today's observed on-site cadence, the pending route's `legMetres` priced
at the truck's measured moving speed — and pulling them a second way is how you end up
with a card reading "~14 installs by 3:45" above "Route done ~6:05" with no arithmetic
connecting them. Deriving it from the identical inputs means the two numbers cannot
disagree; the same reasoning that put `estimate.js` behind all three landing readouts in
the first place.

Two smaller decisions, both the installer's call:

- **It is horizon-independent, so it sits at the top level**, not inside a pace — the same
  rule that keeps the meters/day target out of `paces.target` (a `paces.target.routeFinish`
  would imply a finish clock *per horizon*, which is not a thing).
- **It always reports the real clock.** A day landing at 5:40 says 5:40, in amber
  (`.dp-eta.late`, the `--amber-press` the short-state pill already wears) once it passes
  the card's own `horizonMin` — not "won't finish today". Lateness is measured against the
  card's horizon rather than a wall clock, because that row is now the only horizon on
  screen.

**Verification:** `node --test "tests/*.test.mjs"` — 479/479. New coverage: the finish
clock's arithmetic and label, a finish that lands past both the 3:45 and 4:45 horizons and
is reported as-is rather than clamped, `null` when nothing is pending or the on-site pace
isn't usable yet, and wiring assertions that no `dpTarget` survives in either `drive.js` or
`index.html` while `worklist-tuning.js` still reads `paces.target`.

**And this time the headless-browser pass actually ran**, unlike the three sections above.
`#drive` was driven at a 390 px viewport against a seeded `dayCache` + worklist, and the
card read one gauge — `Day · 3:45`, `~8`, "4 of 24 installs", "Route done ~9:20" — with
`#dpTarget` absent from the DOM. A second run with a route that cannot land (5 stops, 40
min on site, from 2 PM) gave "Route done ~6:10" in `.dp-eta.late`, computed colour
`rgb(201,126,0)` = `--amber-press`, beside "· 4 stops won't fit". The plan-mode banner in
the same fixture still read `~8 by 4:00 · ~8 by 3:45`, which is the untouched
`paces.target` surface doing its job.

The reason the earlier sections say "no browser available" is that the recipe was wrong,
not the sandbox: `VERIFY.md` §3 now records that an agent's Linux sandbox has Chromium at
`/opt/pw-browsers` and needs only `npm i playwright-core`, plus the two things that make a
pace screen drivable at all — block everything that isn't `localhost` at the browser so
the production Sheet is unreachable by construction, and freeze `Date` in an
`addInitScript` **before** the modules load (remembering `stamp()` is Toronto while the
container is UTC, so a frozen 18:00 reads as 14:00).

## The pace gauge measures the route again — and why this morning's reasoning was wrong

`js/compute/estimate.js`, `js/drive.js`, `js/worklist.js`, `js/worklist-tuning.js`,
`tests/estimate.test.mjs`, `tests/pace-live.test.mjs`, `ARCHITECTURE.md`, `AGENTS.md`,
`USER-GUIDE.md`

The installer, on seeing the one-card gauge: *"the pace card should be the pace for the
route and not necessarily for the target — the target just sets how many orders are on the
route."*

That reverses the first section of this very page, so it is worth being precise about what
was wrong with it. `onPace` is `routeShort <= 0` again; `targetShort` becomes the caption's
quiet second half ("· 6 under your 24"). Same flip in the `#tuning` what-if.

**The "route moves underneath the answer" argument does not survive checking.** Day 1 is
`min(capacity + extend, |anchor.ids ∩ pending|)` (`js/route-today.js day1Count`), and a
completed install drops **both** terms by one. So a finished stop moves from `pendingCount`
into `done` and **their sum holds still all day** — the bar does not refill itself and
`routeShort` reports being behind perfectly well. Worse, run the algebra: when the list is
long enough to fill the target, `targetShort = target − done − willDo = pendingCount −
willDo = routeShort`. **The two readings are the same number.** The change bought nothing
except in the one case where it actively hurt — a route *shorter* than the target, where it
told a crew with four orders left in the world that they were "16 short of 24".

The field report it was answering (*"it always says I'm on pace"*) had a second and
sufficient cause, fixed in the same commit and untouched here: the drive phone's stale
`dayCache` held `done` at 0 all day. That is the load-bearing half. This section is why
`AGENTS.md`'s "the denominator is the TARGET" bullet is now a bullet saying the opposite,
with the arithmetic written out — a rule that points the wrong way is how this gets
reverted a third time.

The card now reads, on a short route that lands in full: **on pace**, "4 of 8 stops · 16
under your 24", where it used to read *behind*, "16 short of 24". On one that won't land:
"4 stops short", "4 of 9 stops · 19 under your 24".

### The bug the flip exposed: `todayPending` could pace *tomorrow's* chunk

`todayPending` took the **lowest `day` number present**. Once the meters/day target is met
with orders still pending, `dayCapacity` returns 0, `day1Count` returns 0, and
`scheduleRouteConstraints` stamps **every** remaining order day 2+ — a full day, with the
rest rolled to tomorrow. The lowest day present is then 2.

Reproduced headlessly against the shipped code (24 installed, target 24, 6 pending): the
card read **"24 of 30 stops"** and **"Route done ~3:48"** — today's installs captioned
against six orders nobody is driving today, and a finish clock for them. Round 1's finish
clock inherited the same fault the moment it shipped.

It filters `day === 1` strictly now (blank still sorts as 1). Empty is the honest answer:
`drivePace` returns null and the card hides, because the router has declared today full.

**Verification:** `node --test "tests/*.test.mjs"` — 481/481, plus a headless pass on all
three fixtures at a 390 px viewport (short route lands in full → on pace with the target
footnote; unlandable route → "4 stops short" with the ETA still amber; target met with
orders left → card hidden, and the same fixture reproducing "24 of 30 stops" with the fix
backed out). The plan-mode banner still reads `~8 by 4:00 · ~8 by 3:45`.

---

[← all days](../CHANGELOG.md)
