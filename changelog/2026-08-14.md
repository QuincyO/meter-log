---
title: 2026-08-14
type: changelog
tags: [changelog, project/meter-log, area/spine, incident]
created: 2026-08-14
---

# 2026-08-14 — Outage: Google revoked the script's authorization overnight

`DEPLOY.md` (troubleshooting entry; no code changed)

The crew's phones came up with "cannot connect to the server" this morning — a
**fleet-wide production outage of the spine**, live from sometime between
2026-08-13 ~21:15 (last Sheet write) and ~03:00 Toronto (the nightly
`exportSheetToGithub()` trigger produced no commit). Root cause: **Google had
revoked the script's OAuth grant to execute as its owner.** The web app is
`executeAs: USER_DEPLOYING`, so with the grant gone Google refused every `/exec`
request upstream of `doGet` — HTTP 403 with the Drive *"Access Denied — You need
access"* wall — and the same revocation silently killed the 3am export trigger.
No email announced it (no security alert, no Apps Script failure summary).

The fix was one tap: the owner opened the `/exec` URL in a signed-in browser,
got the re-consent screen, and granted; the very next anonymous request returned
`{ok:true, …}`. Total outage ~15 hours, all of it overnight/morning.

What the diagnosis established, in the order it discriminated:

- **The 403 wall sits in front of our code.** Same page with or without the
  token, any user agent — so `SHARED_TOKEN`, `Code.gs` and the phones were never
  suspects. A Google HTML page at `/exec` means Google-level refusal, full stop.
- **`git` was clean by construction**: `Code.gs`/`appsscript.json` last changed
  2026-08-10 and the app demonstrably worked through the 13th, so no deploy did
  this. The Workspace status dashboard showed no incident; the owner's Drive and
  the Sheet itself were healthy (readable via API, last write 21:15).
- **A redeploy does NOT fix a revoked grant — and its success is itself
  evidence.** The CI workflow was dispatched (`workflow_dispatch`, no commit
  needed); `clasp push` + `clasp deploy -i` succeeded and created version 81
  with the manifest's `ANYONE_ANONYMOUS` — and `/exec` still 403'd. That
  eliminated "the deployment's access flipped" and proved the project existed
  and the owner's *clasp* grant (a separate OAuth client) still worked.
- **The discriminator was the owner opening `/exec` in a signed-in browser.**
  JSON → anonymous-only block; the same wall → Drive ACL; a consent screen →
  revoked authorization. It was the consent screen.

Traps worth remembering:

- **The Drive "You need access" wall at `/exec` can mean a revoked grant, not a
  sharing problem.** Nothing in the console shouts about it, no email arrives,
  and the deployment config reads as perfectly correct the whole time — because
  it is. Send the owner to the `/exec` URL in a signed-in browser *first*; it is
  a 10-second test and when the cause is the grant it is also the fix.
- **The nightly export doubles as a dead-man's switch.** A missing
  `Nightly sheet export — <date>` commit on `main` means the spine's triggers
  were dead overnight — that absence bracketed the outage window before any
  console was opened. Worth checking on any morning report of server trouble.
- **The phones' offline-first design carried the field through.** Capture kept
  queueing to IndexedDB; the standing instruction during any spine outage is
  **do not clear site data / reinstall the PWA** — that destroys the unsent
  queue. Everything drained on its own once the grant was restored.
- Why the grant vanished is **unknown** — no password change, no security
  event, no email. If it recurs, check myaccount.google.com ▸ Security ▸
  Third-party access before anything else, and treat a second occurrence as a
  pattern worth a real dead-man's alarm (the export commit is already the
  signal; something should watch for its absence).

## Verification

`curl "<WEB_APP_URL>?action=roster&token=…"` → `HTTP 200`, `{ok:true, employees:[…]}`
(was: 403 + Google's access-wall HTML, reproduced before and after the CI
redeploy, gone immediately after re-granting). Today's missed export backfills
by running `exportSheetToGithub()` once from the editor, or self-heals at the
next 3am trigger — tomorrow morning's commit landing is the confirmation the
trigger survived.

## Navigate by pin or by address — the installer's switch, not the source's

`index.html`, `js/worklist.js`, `js/worklist-tuning.js`, `css/capture.css`,
`tests/worklist-directions.test.mjs`, `tests/worklist-tuning.test.mjs`,
`ARCHITECTURE.md`, `USER-GUIDE.md`

The 🧭 on a worklist card and the Drive screen's **Navigate** share one function
(`openDirections` → `destOf`), and which destination it hands the maps app has now been
answered both ways in the field: address-first until 2026-08-13, pin-first since. Both
arguments are right some days. The pin is the exact coordinate the route was built
around, so the truck and the route map agree by construction; the address lets Maps
geocode it with knowledge our lookup didn't have, which is the better answer on a new
build or a landmark. Which one wins varies **by day and by address**, so it has stopped
being a source edit and become **Route tuning ▸ Navigate by address instead of map pin**
(`localStorage['wlNavBy']`, default `'pin'` — an untouched phone behaves exactly as it
did before). The hint under it says why you'd switch, because a toggle whose two states
look identical from the outside is a toggle nobody flips on purpose.

Four decisions worth the words:

- **It picks a preference, not a rule.** Each mode falls back to the other when the order
  hasn't got it, so Navigate never becomes a button that does nothing. An order with a pin
  and no address still navigates in address mode; an order with an address and no pin still
  navigates in pin mode.
- **The parked carve-out holds in BOTH modes, and it is structural rather than
  conditional.** `geoFail` (📍 fix address) / `geoAmbig` (⚠ pick a town) orders keep a
  last-known pin that `route.js` never blanks, and that pin is precisely the one already
  known to be in the wrong place. The `isParked` gate therefore stays on the line *above*
  the branch: a parked order simply has no pin to prefer, so neither mode can steer by it.
  **Gate it inside one arm and "coordinates mode" quietly starts driving to known-bad
  pins** — pinned by a test asserting the gate precedes the branch, not just that it exists.
- **One toggle for both surfaces**, because it is one function and one act. Two switches
  would be two things to keep straight in a truck.
- **Device-local, saved on tap, and deliberately not routed through `save()`.** It changes
  nothing the office or the router reads, so it sits in the `.tune-local` block below Save
  with *Show driving stats* — and it must not borrow `save()`'s toast, which promises an
  Upload that would not carry it. It gets its own one-word confirmation instead, since the
  effect is invisible until the next Navigate press.

**Where the accessor lives, and why it isn't next to `destOf`.** `navByPin()`/`setNavByPin()`
are exported from `js/worklist-tuning.js` even though the only reader is `worklist.js`,
because `worklist.js` already imports `initWorklistTuning` from there — the reverse import
would be a cycle (the same reason `js/drive.js` never imports `worklist.js` back). One key,
one default, one place, no duplicated literal.

**No `sw.js` CACHE bump**, on the 2026-08-13 reasoning: no new module or stylesheet, only
edits to files already in `SHELL`, so the rollout stays per-phone via Settings ▸
⟳ Force update rather than every phone re-caching the whole shell at once on boat signal.

### Verification

`node --test "tests/*.test.mjs"` — **655/655** green, including the rewritten
`worklist-directions` tests (the old ones asserted a *source order* — `coords < address` —
which no longer means anything now that both destinations are built and the preference
chooses; they assert the fallback shape and the gate-before-branch rule instead).

Then driven end-to-end in headless Edge (`VERIFY.md` §3, every non-localhost request failed
at the browser per §4 so nothing could reach the production Sheet), with `window.open` and
`navigator.clipboard.writeText` stubbed to record their arguments: **19/19**. Five seeded
orders — clean pin+address, `geoFail`+pin, pin with no address, address with no pin,
`geoAmbig`+pin — pressed on the worklist 🧭 and again on the Drive screen's **Navigate**, in
both modes. Pin mode sent the clean order to `45.3311,-79.2117` and everything flagged or
pinless to `<address>, ON`; address mode sent every addressed order to `<address>, ON` and
fell back to `45.5,-79.4` for the one with no address; both modes, both surfaces copied the
card's address line and never a coordinate pair. The toggle wrote its key on tap with no
Save press and survived a reload.

**A harness trap that reads as a bug in the feature.** The first run failed one check —
"the toggle survives a reload" — and the cause was the drive, not the code: a page loaded
straight at `#tuning` **never opens the tuning screen**, so `loadControls()` never runs and
every control reads as its markup default. The discriminator was the *pre-existing*
`tuneShowDriveMetrics`, which was equally unreflected on that path with its key set;
opened the way the crew does (the ☰ nav's `navTuning`), all three controls painted their
stored values. Worth remembering next to the `#hash`-only navigation trap from 2026-08-13:
a screen this app routes to is not necessarily a screen a URL can land on.

**Noticed while verifying, deliberately not fixed here:** `pullVal(store.get('wlCommutePull'))`
returns **0**, not the documented 70, when the key has never been set — `Number(null)` is `0`
and `isFinite(0)` is true, so the `|| 70` intent never fires. That is the commute-pull weight
`worklist.js planShape` feeds the router on any phone whose installer has not opened Route
tuning and pressed Save. Unrelated to this change and untouched by it; flagged rather than
fixed, since it moves real routes.

## "The End of day button does not work" — one missing element, forty dead handlers

`js/worklist-tuning.js`, `js/pages/capture.js`, `tests/shell-skew-survivable.test.mjs`,
`tests/worklist-tuning.test.mjs`, `AGENTS.md`

Reported from the field as a single dead button. It was not a single dead button: the
capture page had **stopped binding roughly forty handlers**, Log stop among them, and the
crew reported the one they happened to press at the time of day they pressed it.

**The mechanism, end to end.** The Navigate toggle above added `#tuneNavByAddress` to
`index.html` and bound it in `js/worklist-tuning.js` as
`$('tuneNavByAddress').onchange = …`. `sw.js` is stale-while-revalidate **per file** —
every shell file refreshes on its own schedule — so a phone can hold the **new**
`worklist-tuning.js` and the **previous** `index.html` at the same time, and does, for an
open or two after any deploy (indefinitely on a truck, where the background re-fetch
simply fails and the old bytes stay). On such a phone `$('tuneNavByAddress')` is `null`,
`$()` returns null rather than throwing, and the assignment throws a `TypeError`. That
throw surfaced inside `initWorklist()` — which `js/pages/capture.js` calls **at module top
level**, on what is now line 476. A module stops evaluating at its first uncaught
exception, so every handler bound *below* that line was never assigned:

```
TypeError: Cannot set properties of null (setting 'onchange')
    at initWorklistTuning (js/worklist-tuning.js:156)
    at initWorklist       (js/worklist.js:2978)
    at                     js/pages/capture.js:455
```

Reproduced in headless Edge by serving the current `js/` against the previous
`index.html`: **16 of 20** sampled handlers dead — `logStop`, `endDay`, `finishDay`,
`genLog`, `openToday`, `openDowntime`, `saveDowntime`, `openLookup`, `markDone`, the whole
☰ nav and `saveSettings` among them. Only the four bound *above* line 476 survived. The
page looked completely normal; every button on it did nothing.

**And the escape hatch was inside the blast radius.** `#refreshApp` — Settings ▸
⟳ Force update from GitHub, the crew's standing fix for "the app is behaving oddly" — is
bound below the same line, so an affected phone could not force its own repair. That is
the detail that turns an annoyance into a day: the documented recovery path was among the
casualties.

**Two independent fixes, deliberately both.**

1. **The live bug** — `#tuneNavByAddress` is now bound through a null-checked local in
   both `initWorklistTuning` and `loadControls`. A skewed phone loses the Navigate
   toggle and nothing else.
2. **The class of bug** — `initWorklist({ fillCapture })` is wrapped in `try/catch` in
   `capture.js`. The worklist screen is a **sibling** of the capture form, not a
   dependency of it: Log stop, the downtime form, Today, the daily-log PDF and the entire
   end-of-day close-out are all wired below that call, and they are the safety-critical
   path in a field data-capture app. A broken worklist screen must now cost the worklist
   screen only. **Contained is not hidden** — it `console.error`s the real stack for
   whoever debugs it *and* raises an amber notice telling the installer the worklist is
   unavailable, that ⟳ Force update is the fix, and that logging and End of day still
   work. Verified independently of fix 1 by deleting a *different* element (`#wlBack`):
   `initWorklist` throws, the catch fires, all 20 handlers stay bound and End of day
   still opens.

**The rule this earns, now in `AGENTS.md`: an element in a page's markup is a
cross-file contract with the module that binds it.** The 2026-08-13 rule was written as
*"when you add a new module/stylesheet, add it to `SHELL` and bump `CACHE`"*, and the
Navigate commit applied it correctly to the letter — no new module, no new stylesheet, so
no bump. The letter was too narrow. **Adding a new element that JS binds is the same
hazard in different clothes**, because the two halves of the contract live in two files
that the service worker refreshes independently.

**No `sw.js` CACHE bump — and that is now a standing rule, not a per-change call.** The
owner's decision, recorded in `AGENTS.md`: **the constant is never bumped, and agents
should stop proposing it.** Updates reach the field manually, when the installer presses
Settings ▸ ⟳ Force update from GitHub, because that moment belongs to the person holding
the phone rather than to a deploy. The design consequence is the important half: a phone
may sit on older bytes **indefinitely and by choice**, so new-JS-against-old-HTML is not a
brief post-deploy window but an ordinary long-lived state — which makes defensive binding
of freshly-added ids a permanent requirement rather than a one-release courtesy, and makes
the `try`/`catch` above the thing that keeps ⟳ Force update itself reachable.

A third lesson, cheaper to write down than to relearn: **don't infer the blast radius
from the symptom.** One dead button and forty dead buttons are indistinguishable to a crew
that only pressed one, and the report will always name the button they pressed.

### Verification

`node --test "tests/*.test.mjs"` — **659/659** green (655 + 4 new in
`tests/shell-skew-survivable.test.mjs`, which pins the `try`/`catch` with both its
`console.error` and `showNotice` halves, the below-the-init ordering of the close-out
handlers, the guarded binding shape, and the rule's presence in `AGENTS.md`).
`tests/worklist-tuning.test.mjs` was asserting the *unguarded* shape and now asserts the
guarded one.

Driven in headless Edge per `VERIFY.md` §3, with every non-localhost request blocked at
the browser per §4 so nothing could reach the production Sheet, against three servers: the
current tree, the current `js/` over the **previous** `index.html` (the skew), and the
current tree with `#wlBack` deleted (the containment). Before the fix the skew gave
`endDay.onclick === null` and a click that left `#eodSheet` at `display:none`; after it,
all three serve **20/20 handlers bound**, and End of day opens the sheet (`display:flex`,
729×448) in every one — including both broken-markup cases.

## The End-of-day branch finally reaches `main` — and the same trap, caught one element later

`js/pages/capture.js`, `tests/shell-skew-survivable.test.mjs`, plus the merge of
`claude/eod-downtime-sync-bug-4o0wjs` (`ecbc0ac`)

**The tally never worked in the field because it was never in the field.** Reported as
"the snippet didn't paste when I submitted my day", which sounded like a regression and
was not one: `ecbc0ac` was written on 2026-08-13 and **left on its branch**. Pages serves
`main`, so the phones never had a byte of it — `git merge-base --is-ancestor ecbc0ac
origin/main` said no, `tallyBlock` was absent from `main`'s `js/compute/tally.js`, and
`#copyTally` was absent from `main`'s `index.html`. The feature did not break; it had
never run.

That is the second time in two days, and the sharpest detail is where the lesson was
already written: **the warning is inside the unmerged commit.** `ecbc0ac`'s own day page
records the Navigate fix being "shipped once from a feature branch and therefore not
shipped at all". The commit carrying the rule was itself the next thing to break it. The
`AGENTS.md` rule is right and was simply not reached — so when a field report says a
recent feature isn't happening, **check `git merge-base --is-ancestor <commit>
origin/main` before reading any code.** It is one command and it ends the question.

Merged now, with the day's other three unshipped fixes: the EOD review no longer eats
typed deductions (a late `apiGet` repaint that `saveTravel` then posted over Sheet rows —
data loss, not chrome), the PDF's DELAYS grid no longer prints blank when Finish outruns
the prefetch, `await flush()` is a real barrier, and Start/End default to 07:30/16:00.

**And the merge re-introduced this morning's outage, one element further down.** The
branch predates the fix above, so it added `#copyTally` to `index.html` and bound it bare:
`$('copyTally').onclick = …` at `capture.js` top level. Below that line sit `finishDay`,
`genLog`, `openToday`, the whole ☰ nav, `saveSettings` — and **`refreshApp`**. On a phone
holding the new `capture.js` and yesterday's `index.html`, that is a `TypeError` and about
thirteen dead handlers, ⟳ Force update among them, and it would have been reported as
*"the Finish day button does not work"*. Caught before push by applying the rule written
hours earlier; `#copyTally` now goes through a null-checked local, and
`tests/shell-skew-survivable.test.mjs` pins **both** recent ids plus the relationship that
makes it matter — neither may be bound bare above `#refreshApp`, because a handler that
can unbind Force update takes the only route code has to these phones.

The general shape, worth stating once: **a merge is a deploy, and a branch written before
a rule does not know about it.** Re-check merged work against any invariant added while it
sat.

### Verification

`node --test "tests/*.test.mjs"` — **688/688** (659 on `main` + 27 from the branch's four
new test files + 2 new here). Trial-merged first in a throwaway worktree to confirm the
only conflict was `CHANGELOG.md`, both days' rows kept.

Driven in headless Edge per `VERIFY.md` §3 with the spine blocked at the browser (§4), on
a **fresh profile** — the first attempt seeded IndexedDB with a bare
`indexedDB.open('meterlog', 5)` from an init script, which created the database at v5 with
**no object stores**, so the app's own `onupgradeneeded` never fired again and `idb`'s
`.catch(() => null)` swallowed every read for the rest of that profile's life. It printed
as a tally of zeros and looked exactly like a broken feature. Seed through the app's own
`idb` wrapper, on a clean profile.

With six seeded stops (3 INSTALLED, 2 UTI — one `Electrical Repair` — and 1 DONE): the
End-of-day sheet opens, its tally line reads `Installed 3 · UTI 2 · Downtime 0 min ·
1 already-installed`, Start/End default to `07:30`/`16:00`, and 📋 Copy tally puts

```
Dispatched:
Installed: 3
UTI: 2
TR: 0
EER: 1
```

on the clipboard — `Dispatched:` empty on purpose, and `EER: 1` correctly a **subset** of
`UTI: 2`. Then both markup states: current, and the merged `js/` over the **previous**
`index.html` (missing `#copyTally` *and* `#tuneNavByAddress`). **20/20 handlers bound in
both**, no uncaught exceptions in either, End of day opening in both.

---

[← all days](../CHANGELOG.md)
